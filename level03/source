int x = 0;

void v() {

    char buffer[512];

    fget(buffer, 512, 1);

    printf(buffer);

    if (x == 64) {
        fwrite(1, "Wait, what?", 13);
        system('/bin/sh');
    }
}

int main() {
    v()
}